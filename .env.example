# Default course for ./start.sh server bootstrap
SLIDES_COURSE_ID=agentic-ai-for-beginners

# GUI authentication (required for cloud deployment)
GUI_AUTH_USERNAME=admin
GUI_AUTH_PASSWORD=change-me-to-a-strong-password
GUI_SESSION_SECRET=

# Render tuning: reserve cores for GUI/OS/Gentle; cap max Remotion threads
REMOTION_CPU_RESERVE=1
REMOTION_CONCURRENCY=

# Show RunPod option in GUI render dropdowns (default: local-only render)
RENDER_RUNPOD_ENABLED=false

# Internal service URLs (Docker Compose sets gentle/remotion hostnames; scripts also try localhost:8765)
GENTLE_URL=http://gentle:8765
# Browser preview only (local: http://localhost:3000). Not the same as REMOTION_URL below.
REMOTION_STUDIO_URL=http://localhost:3000
# Internal Docker hostname for optional slides-remotion container; do not open in a browser
REMOTION_URL=http://remotion:3000

# Production image (docker-compose.prod.yml)
SLIDES_IMAGE=ghcr.io/f2ka07/ai-video-animation:latest

# API keys used by gui-server (copy from your local .env as needed)
OPENAI_API_KEY=
RUNPOD_API_KEY=
MINIMAX_API_KEY=
MINIMAX_GROUP_ID=
RESEMBLE_API_KEY=
