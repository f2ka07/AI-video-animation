http://localhost:3001/api/health

scp -r ubuntu@ec2-100-58-246-39.compute-1.amazonaws.com:/home/ubuntu/AI-video-animation/out/agentic-ai-for-beginners ./out/a

scp -i "video-key.pem" -r \
ubuntu@ec2-100-58-246-39.compute-1.amazonaws.com:/home/ubuntu/AI-video-animation/out/agentic-ai-for-beginners \
./out/b


time docker exec slides-app npx remotion render src/index.tsx module-2 \
  /app/out/agentic-ai-for-beginners/module-2.mp4 \
  --concurrency=14 --jpeg-quality=80 --timeout=12000

#!/bin/bash
set -e

apt update
apt install -y ca-certificates curl gnupg git unzip

install -m 0755 -d /etc/apt/keyrings

curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
gpg --dearmor -o /etc/apt/keyrings/docker.gpg

chmod a+r /etc/apt/keyrings/docker.gpg

echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu noble stable" \
> /etc/apt/sources.list.d/docker.list

apt update

apt install -y \
docker-ce \
docker-ce-cli \
containerd.io \
docker-buildx-plugin \
docker-compose-plugin

systemctl enable docker
systemctl start docker

usermod -aG docker ubuntu

docker --version
docker compose version
docker run hello-world


