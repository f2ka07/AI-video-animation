from typing import List, Dict

def fix_text(reference_text: str, word_scores: List[Dict]) -> str:
    # Simple: return same text.
    # Later you can drop low-score words or snap to original script.
    return reference_text
